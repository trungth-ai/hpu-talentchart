__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
backend/*.db
.ruff_cache/
.mypy_cache/
.coverage
.venv/
venv/
node_modules/
.next/
.turbo/
.env
.env.production
.env.local
# Anchor về root (/) — pattern không anchor sẽ match mọi cấp,
# từng nuốt nhầm backend/app/data/ (câu hỏi DISC + 12 archetype)
/data/
/logs/
/uploads/
/backups/
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
*.tsbuildinfo

# Dữ liệu nhạy cảm (lương/danh bạ/PII) — TUYỆT ĐỐI KHÔNG commit.
# Đưa lên server bằng scp + docker cp, KHÔNG qua git (commit sẽ nằm vĩnh viễn
# trong history dù xóa sau). Xem docs/DEPLOY.md bước 5.
*.xlsx
*.xls
contacts.csv
/docs/*.csv
